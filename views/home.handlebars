<div class="card">
	<div class="card-header">
		Projects
	</div>
	<div class="card-body">
		<div class="row mb-3">
			{{#each projects}}
			<div class="col-2">
				<a class="btn btn-primary" href="{{.}}">{{.}}</a>
			</div>
			{{/each}}
		</div>
	</div>
</div>